[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = "2.4"
x86_64 = "0.15"
spin = "0.9"
libsys = { path = "../userspace/libsys" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
