[package]
name = "net"
version = "0.1.0"
edition = "2021"

[dependencies]
libsys = { path = "../../userspace/libsys" }
spin = "0.9.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
